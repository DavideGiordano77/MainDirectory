<body>
{{> navbar}}

<section class="actors-container">
    <div class="actors-grid">
        {{#each actors}}
            <div class="actor-card">
                <h2>{{name}}</h2>
                <p class="actor-role">{{role}}</p>
            </div>
        {{/each}}
    </div>
</section>

{{> footer}}
</body>
